cmd_help() {
    echo "Usage: $0 <command> [options]
    Commands:
        init        Initialize the snapshot directory structure.
        backup      Backup files to the snapshot directory.
        restore     Restore files from the snapshot directory.
        install     Install the snapshot tool.
        reinstall   Reinstall the snapshot tool.
        help        Display this help message."
}